import 'package:flutter/material.dart';

class ThemeColors {
  ThemeColors._();

  static Color? grey = Colors.grey[100];

  static const MaterialColor blue = MaterialColor(0xff161c2e, <int, Color>{});
  static const MaterialColor green = MaterialColor(0xffa4fb83, <int, Color>{});
}
